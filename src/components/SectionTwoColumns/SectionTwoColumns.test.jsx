import { render } from '@testing-library/react';
import SectionTwoColumns from '.';

describe('<SectionTwoColumns/>', () => {
  it('should you write something here', () => {
    render(<SectionTwoColumns />);
  });
});
